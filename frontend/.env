# PageWorker API 位址。這個值會被打包進前端 bundle，屬於公開資訊，可進版控。
# 本機若要打自己跑的 wrangler dev，改成 http://localhost:8787 即可
# （Worker 的 CORS 白名單已含 localhost:5173 / 5174）。
VITE_API_BASE=https://pageworker.pageworker.workers.dev
